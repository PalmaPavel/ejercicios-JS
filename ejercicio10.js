function juntar(mensaje1, mensaje2){
   // con el simbolo +, podmos lograr concatenar y que por ende se logren juntar las palabras ya que no tienen el espacio.
   let junto = mensaje1 + mensaje2; 
   console.log(junto);
}

juntar("Pavel", "Palma")
